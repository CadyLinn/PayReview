import SwiftUI

// Kept as a compatibility entry for existing Xcode references.
// The production authentication flow now lives in AppRootView.
struct AuthenticationTestView: View {
    var body: some View {
        AppRootView()
    }
}

#Preview {
    AuthenticationTestView()
}
